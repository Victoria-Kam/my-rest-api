create table author(
                       idauthor serial primary key,
                       nickname varchar(100),
                       age int,
                       email varchar(50),
                       role varchar(20)
)